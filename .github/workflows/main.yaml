name: github actions
on: push 
jobs:
 building:
   runs-on: ubuntu-latest
   steps:
   - name: code checkout
     uses: action/checkout@v4

   - name: maven test
     run: mvn test
   - name: checkstyle
     run: mvn checkstyle:checkstyle

     

     
     
  
